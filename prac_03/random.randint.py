import random

MAX_INCREASE = random.randint(1, 10)  # Random values between 1 and 10
MIN_DECREASE = random.randint(1, 10)   # Random values between 1 and 10

print("Max Increase: {MAX_INCREASE}, Min Decrease: {MIN_DECREASE}")
